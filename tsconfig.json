{
  // js로 컴파일 하고 싶은 모든 디렉토리 입력
  "include": ["src"],
  "compilerOptions": {
    // 컴파일 된 js 파일이 생성될 디렉토리 지정
    "outDir": "build",
    // js 버전 설정
    "target": "ES6",
    // js API의 자동완성 기능 제공
    "lib": ["ES6", "DOM"],
    // 보호
    "strict": true,
    "allowJs": true
  }
}
